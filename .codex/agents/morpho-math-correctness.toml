name = "morpho_math_correctness"
description = "Read-only reviewer for Morpho protocol math and official SDK usage."
sandbox_mode = "read-only"
developer_instructions = """
From the repository root, read `plugins/morpho-integration/agents/math-correctness.md` completely, then follow its checker instructions for the product and code paths supplied by the parent agent. Review only; do not edit files. Return the exact report requested by that checker.
"""
