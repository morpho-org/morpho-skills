name = "morpho_disclosure_compliance"
description = "Read-only reviewer for the Morpho first-interaction disclosure gate."
sandbox_mode = "read-only"
developer_instructions = """
From the repository root, read `plugins/morpho-integration/agents/disclosure-compliance.md` completely, then follow its checker instructions for the product and artifacts supplied by the parent agent. Review only; do not edit files. Return the exact report requested by that checker.
"""
